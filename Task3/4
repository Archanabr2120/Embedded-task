#include <stdio.h>
#include <string.h>

int main() {
    char str[20];
    int i,n=0;
    printf("Enter the String:");
    scanf("%s",str);
    while(str[i] != '\0')
    {
     n=n+1;
     i++;
    }
    printf("The length of the string is %d",n);
    return 0;
}
