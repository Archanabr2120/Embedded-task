#include <stdio.h>

int main() {
    int n,a,i;
    printf("Enter the number: ");
    scanf("%d",&n);
    for(i=2;i<=n;i++)
    {
        a=i%2;
        if(a==0)
        {
            printf("%d\t",i);
        }
    }
    return 0;
}
